var sumar = require('../pilas/sumar');

exports['test'] = function (test) {
    test.equals(1, 1);
    test.done();
};

exports['test2'] = function (test) {
    test.equals(1, 1);
    test.done();
};
